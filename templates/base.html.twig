<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <title>{% block title %}HomeBalance{% endblock %}</title>

  {# ✅ Sneat CSS #}
  <link rel="stylesheet" href="{{ asset('sneat/assets/vendor/fonts/boxicons.css') }}">
  <link rel="stylesheet" href="{{ asset('sneat/assets/vendor/css/core.css') }}">
  <link rel="stylesheet" href="{{ asset('sneat/assets/vendor/css/theme-default.css') }}">
  {# ❌ demo.css = souvent le bleu #}
  {# <link rel="stylesheet" href="{{ asset('sneat/assets/css/demo.css') }}"> #}

  {# ✅ fond clair #}
  <style>
    body { background: #f5f5f9; }
  </style>

  {% block stylesheets %}{% endblock %}

  {# ✅ helpers/config dans le head #}
  <script src="{{ asset('sneat/assets/vendor/js/helpers.js') }}"></script>
  <script src="{{ asset('sneat/assets/js/config.js') }}"></script>

  {% block javascripts %}
    {{ importmap('app') }}
  {% endblock %}
</head>

<body>

{# ✅ LAYOUT GLOBAL (placeholders seulement) #}
<div class="layout-wrapper layout-content-navbar">
  <div class="layout-container">

    {# ✅ SIDEBAR PLACEHOLDER (ton camarade la remplacera) #}
    <aside id="layout-menu" class="layout-menu menu-vertical menu bg-menu-theme">
      <div class="app-brand demo px-3 py-2">
        <span class="app-brand-text demo menu-text fw-bolder">HomeBalance</span>
      </div>

      <div class="menu-inner-shadow"></div>

      <div class="p-3">
        <div class="alert alert-secondary mb-0 py-2">
          Sidebar placeholder
        </div>
      </div>
    </aside>

    <div class="layout-page">

      {# ✅ TOPBAR PLACEHOLDER #}
      <nav class="layout-navbar container-xxl navbar navbar-expand-xl navbar-detached align-items-center bg-navbar-theme">
        <div class="navbar-nav-right d-flex align-items-center w-100">
          <div class="navbar-nav align-items-center">
            <div class="nav-item d-flex align-items-center">
              <i class="bx bx-search fs-4 lh-0 me-2"></i>
              <input class="form-control border-0 shadow-none" placeholder="Search (placeholder)" />
            </div>
          </div>

          <div class="navbar-nav flex-row align-items-center ms-auto">
            <span class="text-muted small">HomeBalance</span>
          </div>
        </div>
      </nav>

      <div class="content-wrapper">
        <div class="container-xxl flex-grow-1 container-p-y">

          {# ✅ Pages existantes de l’équipe (si elles utilisent body) #}
          {% block body %}
            {# ✅ Ton module peut utiliser content #}
            {% block content %}{% endblock %}
          {% endblock %}

        </div>
        <div class="content-backdrop fade"></div>
      </div>
    </div>

  </div>
</div>
<div class="layout-overlay layout-menu-toggle"></div>

{# ✅ Sneat JS (ordre important) #}
<script src="{{ asset('sneat/assets/vendor/libs/jquery/jquery.js') }}"></script>
<script src="{{ asset('sneat/assets/vendor/libs/popper/popper.js') }}"></script>
<script src="{{ asset('sneat/assets/vendor/js/bootstrap.js') }}"></script>
<script src="{{ asset('sneat/assets/vendor/libs/perfect-scrollbar/perfect-scrollbar.js') }}"></script>
<script src="{{ asset('sneat/assets/vendor/js/menu.js') }}"></script>
<script src="{{ asset('sneat/assets/js/main.js') }}"></script>

</body>
</html>
