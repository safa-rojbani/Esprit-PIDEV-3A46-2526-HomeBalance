{% extends 'base_back.html.twig' %}

{% block title %}Add Type{% endblock %}

{% block body %}
<div class="d-flex justify-content-between align-items-center mb-3">
  <h4 class="fw-semibold m-0">Add Event Type</h4>
  <a href="{{ path('admin_type_evenement_index') }}" class="btn btn-outline-secondary">
    Back
  </a>
</div>

<div class="card">
  <div class="card-body">
    {{ include('admin/type_evenement/_form.html.twig', { button_label: 'Create' }) }}
  </div>
</div>
{% endblock %}
