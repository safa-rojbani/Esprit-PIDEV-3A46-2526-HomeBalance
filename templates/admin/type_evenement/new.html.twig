{% extends 'base_back.html.twig' %}

{% block title %}Ajouter un type{% endblock %}

{% block body %}
<div class="d-flex justify-content-between align-items-center mb-3">
  <h4 class="fw-semibold m-0">Ajouter un type d'événement</h4>
  <a href="{{ path('admin_type_evenement_index') }}" class="btn btn-outline-secondary">
    Retour
  </a>
</div>

<div class="card">
  <div class="card-body">
    {{ include('admin/type_evenement/_form.html.twig', { button_label: 'Créer' }) }}
  </div>
</div>
{% endblock %}
