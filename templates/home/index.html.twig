{% extends 'base_front.html.twig' %}

{% block title %}HomeBalance{% endblock %}

{% block body %}
<div class="row">
  <div class="col-12">
    <div class="card">
      <div class="card-body">
        <h4 class="fw-semibold mb-2">Welcome to HomeBalance</h4>
        <p class="text-muted mb-4">Choose where you want to go.</p>

        <a href="{{ path('app_calendar') }}" class="btn btn-primary me-2">
          Go to Client
        </a>
        <a href="{{ path('admin_evenement_index') }}" class="btn btn-outline-secondary">
          Go to Admin
        </a>
      </div>
    </div>
  </div>
</div>
{% endblock %}
