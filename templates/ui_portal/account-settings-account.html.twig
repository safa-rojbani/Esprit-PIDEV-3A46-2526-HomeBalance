{% extends 'ui_portal/layouts/base.html.twig' %}

{% block title %}Paramètres du compte · Informations personnelles{% endblock %}

{% block content %}
  {% include 'ui_portal/partials/account-settings-nav.html.twig' with { active_tab: 'account' } %}

  <p class="text-muted mb-4">Gardez ces informations à jour pour que HomeBalance adapte rappels, budget et attention au rythme de votre foyer.</p>

  {% if accountErrors %}
    <div class="alert alert-danger" role="alert">
      <ul class="mb-0 ps-3">
        {% for error in accountErrors %}
          <li>{{ error }}</li>
        {% endfor %}
      </ul>
    </div>
  {% endif %}

  <div class="card mb-6">
    <form id="formAccountSettings" method="post" action="{{ path('portal_account') }}" enctype="multipart/form-data" novalidate>
      <input type="hidden" name="_token" value="{{ csrf_token('account_update') }}" />
      <input type="hidden" name="section" value="profile" />
      <div class="card-body">
      <div class="d-flex align-items-start align-items-sm-center gap-6 pb-4 border-bottom">
        <img
          src="{{ accountForm.avatarPath ? asset(accountForm.avatarPath) : asset('materio/img/avatars/1.png') }}"
          alt="Avatar utilisateur"
          class="d-block w-px-100 h-px-100 rounded"
          id="uploadedAvatar" />
        <div class="button-wrapper">
          <label for="upload" class="btn btn-primary me-3 mb-4" tabindex="0">
            <span class="d-none d-sm-block">Mettre à jour la photo</span>
            <i class="icon-base bx bx-upload d-block d-sm-none"></i>
            <input
              type="file"
              id="upload"
              class="account-file-input"
              hidden
              accept="image/png, image/jpeg, image/gif, image/webp"
              name="avatar"
              onchange="this.form.submit()" />
          </label>
          <button type="submit" class="btn btn-outline-secondary account-image-reset mb-4" name="avatar_remove" value="1">
            <i class="icon-base bx bx-reset d-block d-sm-none"></i>
            <span class="d-none d-sm-block">Réinitialiser</span>
          </button>
          <div>JPG, PNG, GIF ou WebP, 2 Mo max. Les photos de famille sont les bienvenues.</div>
        </div>
      </div>
      </div>
      <div class="card-body pt-4">
        <div class="row g-6">
          <div class="col-md-6">
            <label for="firstName" class="form-label">Prénom</label>
            <input class="form-control" type="text" id="firstName" name="firstName" value="{{ accountForm.firstName }}" autofocus />
          </div>
          <div class="col-md-6">
            <label for="lastName" class="form-label">Nom</label>
            <input class="form-control" type="text" name="lastName" id="lastName" value="{{ accountForm.lastName }}" />
          </div>
          <div class="col-md-6">
            <div class="d-flex justify-content-between align-items-center">
              <label for="email" class="form-label mb-0">E-mail</label>
              {% if app.user and app.user.emailVerifiedAt %}
                <span class="badge bg-label-success">Vérifié</span>
              {% else %}
                <a href="{{ path('portal_auth_resend_verification') }}" class="small">Vérifier maintenant</a>
              {% endif %}
            </div>
            <input
              class="form-control"
              type="text"
              id="email"
              name="email"
              value="{{ accountForm.email }}"
              placeholder="vous@exemple.com" />
          </div>
          <div class="col-md-6">
            <label for="organization" class="form-label">Foyer ou organisation</label>
            <input type="text" class="form-control" id="organization" name="organization" value="{{ accountForm.organization }}" />
          </div>
          <div class="col-md-6">
            <label class="form-label" for="phoneNumber">Téléphone</label>
            <div class="input-group input-group-merge">
              <span class="input-group-text">+33</span>
              <input
                type="text"
                id="phoneNumber"
                name="phoneNumber"
                class="form-control"
                placeholder="06 12 34 56 78"
                value="{{ accountForm.phoneNumber }}" />
            </div>
          </div>
          <div class="col-md-6">
            <label for="address" class="form-label">Adresse</label>
            <input type="text" class="form-control" id="address" name="address" placeholder="Adresse" value="{{ accountForm.address }}" />
          </div>
          <div class="col-md-6">
            <label for="state" class="form-label">Région</label>
            <input class="form-control" type="text" id="state" name="state" placeholder="Île-de-France" value="{{ accountForm.state }}" />
          </div>
          <div class="col-md-6">
            <label for="zipCode" class="form-label">Code postal</label>
            <input
              type="text"
              class="form-control"
              id="zipCode"
              name="zipCode"
              placeholder="75001"
              maxlength="10"
              value="{{ accountForm.zipCode }}" />
          </div>
          <div class="col-md-6">
            <label class="form-label" for="country">Pays</label>
            <select id="country" class="select2 form-select" name="country">
              <option value="">Sélectionner</option>
              <option value="Australia" {{ accountForm.country == 'Australia' ? 'selected' }}>Australia</option>
              <option value="Bangladesh">Bangladesh</option>
              <option value="Belarus">Belarus</option>
              <option value="Brazil">Brazil</option>
              <option value="Canada">Canada</option>
              <option value="China">China</option>
              <option value="France">France</option>
              <option value="Germany">Germany</option>
              <option value="India">India</option>
              <option value="Indonesia">Indonesia</option>
              <option value="Israel">Israel</option>
              <option value="Italy">Italy</option>
              <option value="Japan">Japan</option>
              <option value="Korea">Korea, Republic of</option>
              <option value="Mexico">Mexico</option>
              <option value="Philippines">Philippines</option>
              <option value="Russia">Russian Federation</option>
              <option value="South Africa">South Africa</option>
              <option value="Thailand">Thailand</option>
              <option value="Turkey">Turkey</option>
              <option value="Ukraine">Ukraine</option>
              <option value="United Arab Emirates">United Arab Emirates</option>
              <option value="United Kingdom" {{ accountForm.country == 'United Kingdom' ? 'selected' }}>United Kingdom</option>
              <option value="United States" {{ accountForm.country == 'United States' ? 'selected' }}>United States</option>
            </select>
          </div>
          <div class="col-md-6">
            <label for="language" class="form-label">Langue</label>
            <select id="language" class="select2 form-select" name="language">
              <option value="">Choisir une langue</option>
              <option value="fr" {{ accountForm.language == 'fr' ? 'selected' }}>Français</option>
              <option value="en" {{ accountForm.language == 'en' ? 'selected' }}>Anglais</option>
              <option value="de" {{ accountForm.language == 'de' ? 'selected' }}>Allemand</option>
              <option value="pt" {{ accountForm.language == 'pt' ? 'selected' }}>Portugais</option>
            </select>
          </div>
          <div class="col-md-6">
            <label for="timeZones" class="form-label">Fuseau horaire</label>
            <select id="timeZones" class="select2 form-select" name="timeZones">
              <option value="">Sélectionner</option>
              <option value="-12" {{ accountForm.timeZones == '-12' ? 'selected' }}>(GMT-12:00) International Date Line West</option>
              <option value="-11" {{ accountForm.timeZones == '-11' ? 'selected' }}>(GMT-11:00) Midway Island, Samoa</option>
              <option value="-10" {{ accountForm.timeZones == '-10' ? 'selected' }}>(GMT-10:00) Hawaii</option>
              <option value="-9" {{ accountForm.timeZones == '-9' ? 'selected' }}>(GMT-09:00) Alaska</option>
              <option value="-8" {{ accountForm.timeZones == '-8' ? 'selected' }}>(GMT-08:00) Pacific Time (US &amp; Canada)</option>
              <option value="-8">(GMT-08:00) Tijuana, Baja California</option>
              <option value="-7">(GMT-07:00) Arizona</option>
              <option value="-7">(GMT-07:00) Chihuahua, La Paz, Mazatlan</option>
              <option value="-7">(GMT-07:00) Mountain Time (US &amp; Canada)</option>
              <option value="-6">(GMT-06:00) Central America</option>
              <option value="-6">(GMT-06:00) Central Time (US &amp; Canada)</option>
              <option value="-6">(GMT-06:00) Guadalajara, Mexico City, Monterrey</option>
              <option value="-6">(GMT-06:00) Saskatchewan</option>
              <option value="-5">(GMT-05:00) Bogota, Lima, Quito, Rio Branco</option>
              <option value="-5">(GMT-05:00) Eastern Time (US &amp; Canada)</option>
              <option value="-5">(GMT-05:00) Indiana (East)</option>
              <option value="-4">(GMT-04:00) Atlantic Time (Canada)</option>
              <option value="-4">(GMT-04:00) Caracas, La Paz</option>
            </select>
          </div>
          <div class="col-md-6">
            <label for="currency" class="form-label">Devise</label>
            <select id="currency" class="select2 form-select" name="currency">
              <option value="">Sélectionner</option>
              <option value="euro" {{ accountForm.currency == 'euro' ? 'selected' }}>Euro</option>
              <option value="usd" {{ accountForm.currency == 'usd' ? 'selected' }}>Dollar US</option>
              <option value="pound" {{ accountForm.currency == 'pound' ? 'selected' }}>Livre sterling</option>
              <option value="bitcoin" {{ accountForm.currency == 'bitcoin' ? 'selected' }}>Bitcoin</option>
            </select>
          </div>
        </div>
        <div class="mt-6">
          <button type="submit" class="btn btn-primary me-3">Enregistrer</button>
          <button type="reset" class="btn btn-outline-secondary">Réinitialiser</button>
        </div>
      </div>
    </form>
  </div>

  <div class="card mb-6">
    <div class="card-header">
      <h5 class="mb-0">Mot de passe et sécurité</h5>
      <small class="text-muted">Changez-le dès qu’un doute apparaît.</small>
    </div>
    <div class="card-body">
      <form method="post" action="{{ path('portal_account') }}" class="row g-6" novalidate>
        <input type="hidden" name="_token" value="{{ csrf_token('account_password') }}" />
        <input type="hidden" name="section" value="password" />
        <div class="col-md-4">
          <label for="currentPassword" class="form-label">Mot de passe actuel</label>
          <input type="password" class="form-control" id="currentPassword" name="currentPassword" autocomplete="current-password" />
        </div>
        <div class="col-md-4">
          <label for="newPassword" class="form-label">Nouveau mot de passe</label>
          <input type="password" class="form-control" id="newPassword" name="newPassword" autocomplete="new-password" />
        </div>
        <div class="col-md-4">
          <label for="confirmPassword" class="form-label">Confirmer le mot de passe</label>
          <input type="password" class="form-control" id="confirmPassword" name="confirmPassword" autocomplete="new-password" />
        </div>
        <div class="col-12 d-flex align-items-center">
          <button type="submit" class="btn btn-primary me-3">Mettre à jour</button>
          <small class="text-muted">8 caractères minimum, idéalement avec un symbole ou un chiffre.</small>
        </div>
      </form>
    </div>
  </div>

  <div class="card">
    <h5 class="card-header">Besoin d’une pause ?</h5>
    <div class="card-body">
      <div class="mb-6 col-12 mb-0">
        <div class="alert alert-warning">
          <h5 class="alert-heading mb-1">Supprimer votre compte efface l’historique du foyer.</h5>
          <p class="mb-0">Prenez un instant : une fois supprimé, l’historique ne pourra pas être restauré.</p>
        </div>
      </div>
      <form id="formAccountDeactivation" method="post" action="{{ path('portal_account_delete') }}" novalidate>
        <input type="hidden" name="_token" value="{{ csrf_token('account_delete') }}" />
        <div class="form-check my-8 ms-2">
          <input class="form-check-input" type="checkbox" name="accountActivation" id="accountActivation" />
          <label class="form-check-label" for="accountActivation">Je comprends que mon compte sera supprimé</label>
        </div>
        <button type="submit" class="btn btn-danger deactivate-account">Supprimer mon compte</button>
      </form>
    </div>
  </div>

  <div class="card mt-6">
    <div class="card-header d-flex justify-content-between align-items-center">
      <div>
        <h5 class="mb-0">Badges et reconnaissances</h5>
        <small class="text-muted">Un aperçu des réussites personnelles et familiales.</small>
      </div>
    </div>
    <div class="card-body">
      <div class="row g-4">
        <div class="col-lg-6">
          <h6 class="fw-semibold mb-2">Vos badges</h6>
          {% if metrics.badges is empty %}
            <p class="text-muted">Pas encore de badge. Gardez le rythme pour les débloquer.</p>
          {% else %}
            <ul class="list-unstyled mb-0">
              {% for badge in metrics.badges %}
                <li class="mb-3">
                  <div class="fw-semibold">{{ badge.name }}</div>
                  <small class="text-muted">{{ badge.description ?? 'Obtenu grâce à une progression régulière.' }}</small>
                </li>
              {% endfor %}
            </ul>
          {% endif %}
        </div>
        <div class="col-lg-6">
          <h6 class="fw-semibold mb-2">Badges de la famille</h6>
          {% if metrics.familyBadges is empty %}
            <p class="text-muted">Invitez un proche pour débloquer les badges d’équipe.</p>
          {% else %}
            <ul class="list-unstyled mb-0">
              {% for badge in metrics.familyBadges %}
                <li class="mb-3">
                  <div class="fw-semibold">{{ badge.name }}</div>
                  <small class="text-muted">
                    {{ badge.description ?? 'Reconnaissance partagée par tous' }}
                    {% if badge.awardedAt %}
                      · attribué le {{ badge.awardedAt|date('d M') }}
                    {% endif %}
                  </small>
                </li>
              {% endfor %}
            </ul>
          {% endif %}
        </div>
      </div>
    </div>
  </div>
{% endblock %}

{% block extra_javascripts %}
  {{ parent() }}
  <script src="{{ asset('js/pages-account-settings-account.js') }}"></script>
{% endblock %}
