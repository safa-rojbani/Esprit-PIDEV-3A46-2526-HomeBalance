{% extends 'ui_portal/layouts/base.html.twig' %}

{% block title %}Admin · Invitations{% endblock %}

{% block content %}
  <div class="d-flex align-items-center justify-content-between flex-wrap gap-3 mb-4">
    <div>
      <h4 class="mb-0">Invitations familiales</h4>
      <p class="text-muted mb-0">Suivez le cycle des invitations et codes d’accès.</p>
    </div>
    <div class="d-flex gap-2 flex-wrap">
      <a class="btn btn-primary" href="{{ path('portal_admin_invitations_new') }}">Nouvelle invitation</a>
      <form class="d-flex" method="get" action="{{ path('portal_admin_invitations_index') }}">
        <input type="search" class="form-control me-3" placeholder="E-mail ou code" name="q" value="{{ filters.query }}" />
        <select class="form-select me-3" name="sort">
          <option value="createdAt" {{ filters.sort == 'createdAt' ? 'selected' }}>Plus récentes</option>
          <option value="status" {{ filters.sort == 'status' ? 'selected' }}>Statut</option>
          <option value="email" {{ filters.sort == 'email' ? 'selected' }}>E-mail</option>
        </select>
        <select class="form-select me-3" name="dir">
          <option value="DESC" {{ filters.direction == 'DESC' ? 'selected' }}>Desc</option>
          <option value="ASC" {{ filters.direction == 'ASC' ? 'selected' }}>Asc</option>
        </select>
        <button class="btn btn-primary" type="submit">Appliquer</button>
      </form>
    </div>
  </div>

  <div class="card">
    <div class="table-responsive text-nowrap">
      <table class="table">
        <thead>
          <tr>
            <th>E-mail</th>
            <th>Code</th>
            <th>Statut</th>
            <th>Famille</th>
            <th>Expiration</th>
            <th class="text-end">Actions</th>
          </tr>
        </thead>
        <tbody>
          {% if invitations is empty %}
            <tr>
              <td colspan="6" class="text-center text-muted py-6">Aucune invitation trouvée.</td>
            </tr>
          {% else %}
            {% for invitation in invitations %}
              <tr>
                <td>{{ invitation.invitedEmail ?? '—' }}</td>
                <td>{{ invitation.joinCode }}</td>
                <td>{{ invitation.status ? invitation.status.name|title : '—' }}</td>
                <td>{{ invitation.family ? invitation.family.name : '—' }}</td>
                <td>{{ invitation.expiresAt ? invitation.expiresAt|date('d/m/Y H:i') : '—' }}</td>
                <td class="text-end">
                  <div class="btn-group">
                    <a href="{{ path('portal_admin_invitations_edit', { id: invitation.id }) }}" class="btn btn-sm btn-outline-secondary">Modifier</a>
                    <form method="post" action="{{ path('portal_admin_invitations_delete', { id: invitation.id }) }}">
                      <input type="hidden" name="_token" value="{{ csrf_token('delete_invitation_' ~ invitation.id) }}" />
                      <button class="btn btn-sm btn-outline-danger" type="submit">Supprimer</button>
                    </form>
                  </div>
                </td>
              </tr>
            {% endfor %}
          {% endif %}
        </tbody>
      </table>
    </div>
    <div class="card-footer d-flex justify-content-between align-items-center">
      <div class="text-muted">Affichage {{ paginationMeta.first }}-{{ paginationMeta.last }} sur {{ paginationMeta.total }} invitations</div>
      <div>{{ knp_pagination_render(pagination) }}</div>
    </div>
  </div>
{% endblock %}
