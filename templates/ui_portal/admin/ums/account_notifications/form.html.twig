{% extends 'ui_portal/layouts/base.html.twig' %}

{% block title %}Admin · {{ title }}{% endblock %}

{% block content %}
  <div class="d-flex align-items-center justify-content-between flex-wrap gap-3 mb-4">
    <div>
      <h4 class="mb-0">{{ title }}</h4>
      <p class="text-muted mb-0">Edit notification delivery details.</p>
    </div>
    <a href="{{ path('portal_admin_account_notifications_index') }}" class="btn btn-outline-secondary">Back to list</a>
  </div>

  {% if formErrors %}
    <div class="alert alert-danger" role="alert">
      <ul class="mb-0 ps-3">
        {% for error in formErrors %}
          <li>{{ error }}</li>
        {% endfor %}
      </ul>
    </div>
  {% endif %}

  <div class="card">
    <div class="card-body">
      {{ form_start(form, { attr: { novalidate: 'novalidate' } }) }}
      <div class="row g-3">
        <div class="col-md-6">{{ form_row(form.user) }}</div>
        <div class="col-md-6">{{ form_row(form.key) }}</div>
        <div class="col-md-4">{{ form_row(form.channel) }}</div>
        <div class="col-md-4">{{ form_row(form.status) }}</div>
        <div class="col-md-6">{{ form_row(form.createdAt) }}</div>
        <div class="col-md-6">{{ form_row(form.sentAt) }}</div>
        <div class="col-md-12">{{ form_row(form.lastError) }}</div>
      </div>
      <div class="mt-4 d-flex justify-content-end gap-2">
        <a href="{{ path('portal_admin_account_notifications_index') }}" class="btn btn-outline-secondary">Cancel</a>
        <button class="btn btn-primary" type="submit">Save</button>
      </div>
      {{ form_end(form) }}
    </div>
  </div>
{% endblock %}
