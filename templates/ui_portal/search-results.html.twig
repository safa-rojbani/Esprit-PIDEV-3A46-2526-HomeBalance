{% extends 'ui_portal/layouts/base.html.twig' %}

{% block title %}HomeBalance · Recherche{% endblock %}

{% block content %}
  <div class="d-flex align-items-center justify-content-between flex-wrap gap-3 mb-4">
    <div>
      <h4 class="mb-0">Résultats de recherche</h4>
      <p class="text-muted mb-0">Recherche UMS : utilisateurs, familles, invitations et badges.</p>
    </div>
  </div>

  {% if query == '' %}
    <div class="alert alert-info">Saisissez un terme pour commencer.</div>
  {% else %}
    <div class="alert alert-light">
      Résultats pour <strong>{{ query }}</strong>
    </div>
  {% endif %}

  {% set hasResults = false %}
  {% for list in results %}
    {% if list is not empty %}
      {% set hasResults = true %}
    {% endif %}
  {% endfor %}

  {% if query != '' and not hasResults %}
    <div class="alert alert-warning">Aucun résultat pour cette recherche.</div>
  {% endif %}

  {% if results.users is not empty %}
    <div class="card mb-4">
      <div class="card-header"><h5 class="mb-0">Utilisateurs</h5></div>
      <div class="card-body">
        <ul class="mb-0">
          {% for user in results.users %}
            <li>{{ user.getUserIdentifier() }} · {{ user.getEmail() }}</li>
          {% endfor %}
        </ul>
      </div>
    </div>
  {% endif %}

  {% if results.families is not empty %}
    <div class="card mb-4">
      <div class="card-header"><h5 class="mb-0">Familles</h5></div>
      <div class="card-body">
        <ul class="mb-0">
          {% for family in results.families %}
            <li>{{ family.getName() }} · {{ family.getJoinCode() ?? 'Sans code' }}</li>
          {% endfor %}
        </ul>
      </div>
    </div>
  {% endif %}

  {% if results.invitations is not empty %}
    <div class="card mb-4">
      <div class="card-header"><h5 class="mb-0">Invitations</h5></div>
      <div class="card-body">
        <ul class="mb-0">
          {% for invite in results.invitations %}
            <li>{{ invite.getInvitedEmail() }} · {{ invite.getJoinCode() }} · {{ invite.getStatus().name|title }}</li>
          {% endfor %}
        </ul>
      </div>
    </div>
  {% endif %}

  {% if results.memberships is not empty %}
    <div class="card mb-4">
      <div class="card-header"><h5 class="mb-0">Adhésions</h5></div>
      <div class="card-body">
        <ul class="mb-0">
          {% for membership in results.memberships %}
            <li>{{ membership.getUser().getUserIdentifier() }} · {{ membership.getFamily().getName() }} · {{ membership.getRole().name|title }}</li>
          {% endfor %}
        </ul>
      </div>
    </div>
  {% endif %}

  {% if results.badges is not empty %}
    <div class="card mb-4">
      <div class="card-header"><h5 class="mb-0">Badges</h5></div>
      <div class="card-body">
        <ul class="mb-0">
          {% for badge in results.badges %}
            <li>{{ badge.getName() }} · {{ badge.getCode() }}</li>
          {% endfor %}
        </ul>
      </div>
    </div>
  {% endif %}

  {% if results.familyBadges is not empty %}
    <div class="card mb-4">
      <div class="card-header"><h5 class="mb-0">Badges famille</h5></div>
      <div class="card-body">
        <ul class="mb-0">
          {% for record in results.familyBadges %}
            <li>{{ record.getFamily().getName() }} · {{ record.getBadge().getName() }}</li>
          {% endfor %}
        </ul>
      </div>
    </div>
  {% endif %}

  {% if results.accountNotifications is not empty %}
    <div class="card mb-4">
      <div class="card-header"><h5 class="mb-0">Notifications compte</h5></div>
      <div class="card-body">
        <ul class="mb-0">
          {% for notification in results.accountNotifications %}
            <li>{{ notification.getUser().getUserIdentifier() }} · {{ notification.getKey() }} · {{ notification.getStatus() }}</li>
          {% endfor %}
        </ul>
      </div>
    </div>
  {% endif %}

  {% if results.auditTrails is not empty %}
    <div class="card mb-4">
      <div class="card-header"><h5 class="mb-0">Journal d’audit</h5></div>
      <div class="card-body">
        <ul class="mb-0">
          {% for record in results.auditTrails %}
            <li>{{ record.getAction() }} · {{ record.getUser() ? record.getUser().getUserIdentifier() : 'Système' }}</li>
          {% endfor %}
        </ul>
      </div>
    </div>
  {% endif %}
{% endblock %}
