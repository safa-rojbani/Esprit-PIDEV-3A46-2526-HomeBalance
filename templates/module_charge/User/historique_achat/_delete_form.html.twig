<form method="post" action="{{ path('app_historique_achat_delete', {'id': historique_achat.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ historique_achat.id) }}">
    <button class="btn">Supprimer</button>
</form>
