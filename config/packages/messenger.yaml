framework:
  messenger:
    failure_transport: failed

    transports:
      async:
        dsn: '%env(MESSENGER_TRANSPORT_DSN)%'
        retry_strategy:
          max_retries: 3
          multiplier: 2

      # ✅ transport dédié au scheduler
      scheduler_default: 'doctrine://default?queue_name=scheduler'

      failed: 'doctrine://default?queue_name=failed'

    default_bus: messenger.bus.default

<<<<<<< HEAD
    buses:
      messenger.bus.default: []

    routing:
      # ✅ notre message de purge va dans scheduler_default
      App\Message\PurgeTrashMessage: scheduler_default

      Symfony\Component\Mailer\Messenger\SendEmailMessage: async
      Symfony\Component\Notifier\Message\ChatMessage: async
      Symfony\Component\Notifier\Message\SmsMessage: async
=======
            App\Message\SendAccountNotification: async
            App\Message\TaskCompleted: async

            # Route your messages to the transports
            # 'App\Message\YourMessage': async

when@dev:
    framework:
        messenger:
            transports:
                async: 'sync://'
>>>>>>> origin/main
